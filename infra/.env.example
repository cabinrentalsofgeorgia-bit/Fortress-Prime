# Copy to repo root as .env (chmod 600). Never commit .env.
# Used by vLLM scripts and docker-compose; HF_TOKEN for gated models.

HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# DB_PASS=...  # If running DB-dependent services from infra

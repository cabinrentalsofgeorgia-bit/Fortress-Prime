--- /home/admin/Fortress-Prime/litellm_config.yaml.bak.wave-5-6-20260501T135222Z	2026-05-01 09:52:22.527469977 -0400
+++ /home/admin/Fortress-Prime/litellm_config.yaml	2026-05-01 09:52:38.443701472 -0400
@@ -171,6 +171,32 @@
         enable_thinking: false
 
   # ──────────────────────────────────────────────────────────────────────────
+  # legal-faithfulness — Wave 5.6 (2026-05-01)
+  # Same upstream model as legal-reasoning (nemotron-3-super on the spark-3+4
+  # frontier). Tuned for the RAG faithfulness rail's structured-output
+  # classification task: deterministic temperature, room for a 5-dimension
+  # JSON object with claim lists, thinking disabled.
+  # Why a separate alias from legal-reasoning: legal-reasoning has
+  # enable_thinking=true baked in — extra_body overrides on the request side
+  # do NOT propagate through LiteLLM to vLLM (verified empirically), so a
+  # call to legal-reasoning with thinking-off intent still gets a thinking
+  # trace prepended and exhausts max_tokens before producing JSON.
+  # Why a separate alias from legal-moderation: max_tokens=64 is far too
+  # small for a JSON object listing claims; the faithfulness rail needs
+  # ~2000 tokens for sections with many claims.
+  # ──────────────────────────────────────────────────────────────────────────
+  - model_name: legal-faithfulness
+    litellm_params:
+      model: openai/nemotron-3-super
+      api_base: http://10.10.10.3:8000/v1
+      api_key: EMPTY
+      temperature: 0.0
+      max_tokens: 2000
+      timeout: 600
+      chat_template_kwargs:
+        enable_thinking: false
+
+  # ──────────────────────────────────────────────────────────────────────────
   # CLOUD ROUTES — available for NON-LEGAL use only after ADR-003 Phase 1.
   # Legal traffic terminates on the sovereign legal-* routes above.
   # These routes remain active because non-legal callers (storefront, command
